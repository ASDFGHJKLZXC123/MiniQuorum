# MiniQuorum run log

| Date | Packet | Branch / commit | Worker outcome | Verifier outcome | Gate evidence |
|---|---|---|---|---|---|
| 2026-07-27 | Phase 4 exit | Portability correction `codex/phase4-ci-portability` / `a7202f749dc0201a614a772de50a783f62b65e33` (parent `d5fe2dbad966f67a8c501a1b027b8b97a56501c1`); public PR [#1](https://github.com/ASDFGHJKLZXC123/MiniQuorum/pull/1); merged to `main` as `4d1116c1d5334534222c2590ddf5562204ab03e5` | PASS — fresh GPT-5.6 Sol (`gpt-5.6-sol`) xhigh correction worker fixed Darwin/arm64 versus Linux/amd64 corpus drift with explicit `math.FMA`, added exact-bit and raw-byte portability diagnostics, made both corpus reproduction and replay gates explicit, and added the Linux preflight. It changed only six packet-4C code/configuration files, retained generator v5 and every committed corpus byte, edited no checklist or protected file, and used no fallback model. | Unconditional PASS — independent read-only Claude Opus 5 (`claude-opus-5`) max, session `9375920c-02c1-42f5-b8f3-9b6ae19006d8`, reviewed the complete Phase 4 and correction diffs, independently inspected the hosted evidence, reran the local gates, changed nothing, and found no Critical, High, or Medium issue. Its authoritative detached full-race bundle ended `VERIFIER_LONG_GATES_PASS` at the exact candidate after 2h03m05s (`internal/simharness` 7381.051s); earlier SIGTERM-interrupted verifier subprocesses were orchestration failures and counted as neither pass nor fail. | Worker, orchestrator, and verifier independently passed whitespace/formatting, module integrity, build, ordinary and integration vet, lint (0 issues), protobuf reproducibility, boundary plus detector self-tests, all-100 generated-v5 byte reproduction under ordinary/race builds, the seed-5 exact-bits canary (`0x3fe926f0730a3a43`; schedule SHA-256 `ef83546b9aa80a2c78e1014a0242d363ccb166b5338a1bd59e1c64f35e9276d3`), all 104 corpus replays, tagged negative control, `sim-500`, `sim-crash-500`, local 1k, real failover/SIGKILL smokes, and the unchanged full race suite. Hosted PR CI [30314297594](https://github.com/ASDFGHJKLZXC123/MiniQuorum/actions/runs/30314297594) and push CI [30314295404](https://github.com/ASDFGHJKLZXC123/MiniQuorum/actions/runs/30314295404) passed Linux/amd64 preflight, validate, and 1,000/1,000 simulations. Hosted Nightly [30314328792](https://github.com/ASDFGHJKLZXC123/MiniQuorum/actions/runs/30314328792) covered disjoint seeds 1–10,000 in four shards: 10,000 passed, 0 failed, 2,000,000 logical and completed operations, minima 200/200, all PUT/GET/DELETE and fault/crash kinds, 764,181 real drops, 1,366,744 duplicates, and 121,053 in-flight faults. Its buggy-tag replay exited 1 specifically with `checker_ran=true`, `linearizable=false`, and Porcupine violation evidence. Slowest nightly shard was 1h43m31s, safely within the configured 6h CI window although slower than the phase's approximate 45–50m estimate; that variance is recorded as a non-blocking smell. Evidence marks exactly seven remaining boxes: CI 1k/10k/negative; real local 1k plus nightly 10k; runtime within the CI window; zero violations; committed versioned corpus; all three scripted shapes; and no free-running sim goroutines. Phase 4 verdict: **PASS**; recommend Richard approve Phase 5 when ready. |
| 2026-07-26 | 4C | `phase-4/4C`; approved replay-decision commit `335df8d226d5b1712bfba21a152fe2c5506cf487`; candidate `ed851739e655a276a339d059fc32d00e57ad2ef6`; merged locally to `main` as `e06c2ef4b7395eba946961c8ac17b39409775a83` (baseline `473fc097994b2a0a423d5bd3ca0f2b29dedd8e8d`) | PASS after an explicitly recorded cross-provider continuation. The assigned GPT-5.6 Sol (`gpt-5.6-sol`) xhigh worker was interrupted twice by an erroneous content classifier; its partial work was preserved. The documented Claude Fable 5 (`claude-fable-5`) max fallback, session `685c1a4b-d785-4f9b-9c57-8b9b1c8313a2`, advanced the packet until its usage limit. Richard explicitly approved Claude Opus as the replacement; Claude Opus 4.8 (`claude-opus-4-8`) high sessions `410a3972-6a20-4dea-aa04-b945adc6594f` and `752a7f7a-eca2-4113-973a-3f126f9229bb`, followed by max corrective session `9191c841-2de3-4199-8add-8340a6e3ff4d`, completed the implementation and audit corrections. No Haiku and no silent substitution. | Packet implementation PASS; Phase 4 exit HOLD — independent read-only Claude Opus 5 (`claude-opus-5`) at max effort, session `22d4c4c2-1629-4099-8880-e8d2de0bbc23`, explicitly approved by Richard in place of the unavailable Fable allocation. It reran every local gate, reviewed the complete diff, changed no files, and found no Critical, High, or Medium issues. Four Low reporting/evidence-precision findings and three informational observations were non-blocking. | Worker, orchestrator, and verifier independently passed formatting/whitespace, module integrity, build, ordinary and integration vet, lint (0 issues), boundary plus forbidden-`time` and bare-Go negative probes, byte-identical protobuf regeneration, `sim-500`, `sim-crash-500`, all 104 corpus schedules, all 100 generated-v5 byte reproductions, real leader-failover and SIGKILL/restart smokes, pinned ordinary replay, tagged negative control, and complete uncached race testing. Verifier race: every package green, `internal/simharness` 6428.061s, wall 6430s. Verifier 1k: 1000/1000, 200,000 operations, zero failures, 12,020 in-flight faults. Verifier 10k: 10000/10000, 2,000,000 operations, zero failures, 121,053 in-flight faults, compute time 3h11m0.72s; shell wall included host sleep. Both tagged CLI replays exited 1 specifically on Porcupine, were byte-identical, and matched the committed pin; SHA-256: schedule `7ae83dac6d1e802a4c12549fa670922d643cab340d10a37c8512b692f4ec5a11`, history `962465d0636d2bfebbc9bdbaf8a452011aca5c9a10e3efde77a8c206e04dfe42`, trace `d97b05971f33c92ba8d0d3f0da28a0dff050c129f4f876bdf749e7b02c9b4c34`, HTML `e08b9efc20cee3ffc4512c1cfd56ac1d2e0567e7aef535879a9fa4839de68b84`. Evidence checks exactly seven 4C boxes: seed replay, tagged negative-control seam, interleaving sanity, timeout handling, deterministic history, Porcupine rejection with committed pin, and identical replay/visualization. The CI build-path box, compound local-plus-nightly 10k box, runtime-budget box, and all four exit boxes remain open. |
| 2026-07-26 | Phase 4 exit attempt | `main` / locally verified packet merge `e06c2ef4b7395eba946961c8ac17b39409775a83` (Phase 4 opening baseline `926c525`) | Packets locally integrated: 4A Claude Sonnet 5 high with GPT-5.6 Sol max correction; 4B GPT-5.6 Sol max; 4C assigned GPT-5.6 Sol xhigh with explicit Fable 5 max then Opus 4.8 high/max continuation after classifier and usage interruptions; no silent substitutions | **HOLD**, not a correctness failure — Claude Opus 5 (`claude-opus-5`) max, read-only, session `22d4c4c2-1629-4099-8880-e8d2de0bbc23`. The implementation and all local exit claims passed, but no remote exists and no hosted workflow was authorized or run. | Local evidence supports zero violations across the full 10k campaign, the committed 100-generated plus 3-scripted plus 1-pinned versioned corpus, all three required scripted shapes, and a synchronous deterministic sim path. Those four exit boxes are deliberately left unchecked while the phase verdict is HOLD. Missing evidence: hosted PR `validate` and 1k jobs; all four hosted nightly 10k shards; hosted nightly negative-control job; pinned-CI-version lint parity; hosted `make test` runtime margin; and the nightly runtime-budget claim. Local measurement was 9.17 core-seconds/seed, about 9× the phase file's estimate, so hosted timing must be observed rather than inferred. CI link: N/A — local-only repository; no remote or push by instruction. Recommendation: HOLD Phase 5 until Richard authorizes a remote/hosted run or explicitly decides how to resolve the CI-only gate. |
| 2026-07-18 | 4B | `phase-4/4B` / initial `d89d7ae`, evidence strengthening `84a2828`, post-4A integration `9c6174fdd007aad1ce44967dedb093a9c26859fd`, merged to `main` as `2445c01` (main parent `a9bc37b`) | PASS — launch metadata selected GPT-5.6 Sol (`gpt-5.6-sol`) max, no fallback, session `019f74ff-88fb-7b11-b256-128dbe06272a`; its internal self-description exposed only generic GPT-5 Codex, so the exact launcher metadata and self-attestation limitation are recorded without claiming a substitution. A prior exact launch stopped before repository work because it would not attest the model label. The worker merged 4A into 4B, resolved only `sim/event.go` and `sim/ready.go`, and preserved both fault and workload paths. | Unconditional PASS — Claude Opus 4.8 (`claude-opus-4-8`), high effort by launch command, read-only, no fallback, session `62c7e057-e4f8-4f83-aeac-9f7b25224c37`; runtime metadata identified the assigned model and the verifier noted effort is not internally introspectable. | Worker, orchestrator, and verifier independently passed complete scope/protected-tree/merge-resolution audits; build; ordinary and integration vet; lint (0 issues); tidy/gofmt/whitespace; boundary and forbidden-construct self-tests; byte-identical protobuf regeneration; full race suite; focused checker/workload/fault race repetition; fixed-seed reproducibility; `sim-500`; `sim-crash-500`; corpus; and real three-process leader-failover smoke. Exact `Save → send → apply → Advance`, Save-error suppression, Apply-error fail-stop before workload observation, and one-live-tick-stream behavior passed inspection and regression tests. Approved Porcupine v1.3.0 is the only dependency added. Disposable probes produced 60/60 linearizable faulted-workload seeds (2,347 completed, 11 open), byte-identical 28,136-byte same-seed histories, 11/11 rejected corrupt GETs, and correct missing/empty separation; 9/11 valid mutations were killed. Non-blocking findings: equal return/invoke timestamps are deliberately treated as concurrent and occurred on the same key 5 times in 3,193 sampled ops; apply-before-Advance and the superseded-attempt guard are correct but not mutation-enforced; timed-out sessions need explicit retry; committed tests do not yet combine faults with workloads. Evidence checks only the four 4B build-path boxes and the Porcupine-model unit-test box; heavy-fault timeout, interleaving, history-file determinism, replay, negative-control, CI, 10k, and exit boxes remain for 4C. |
| 2026-07-18 | 4A | `phase-4/4A` / initial `230ce72`, corrections `2e3f131`, `eeea410`, final `0b96c8eb5a322ab0207ba4e47c85f32954c17137`, merged to `main` as `4049cd3` (baseline `926c525`) | PASS after correction cycles — initial Claude Sonnet 5 high; final corrective launch explicitly selected GPT-5.6 Sol (`gpt-5.6-sol`) max with no fallback, while its runtime self-label exposed only generic GPT-5 Codex and could not attest the Sol label. The command selection and limitation are recorded without claiming a substitution. | Unconditional PASS — Claude Opus 4.8 (`claude-opus-4-8`), high effort by launch command, read-only, no fallback; runtime metadata explicitly identified `claude-opus-4-8` and the verifier noted that effort is not introspectable from inside the session. | Worker, orchestrator, and verifier independently passed scope/protected-tree/Ready-order audits; build; ordinary and integration vet; lint (0 issues); full race suite; boundary; tidy/gofmt/whitespace; fixed seed; `sim-500`; `sim-crash-500`; corpus command; generator properties; behavior canaries; and focused Save/Apply fail-stop plus stale-tick race stress. Static adversarial generation covered 21,000 schedules; runtime matrices covered 2,400 fault runs and 2,000 tick-stream runs with 1,773,997 tick events and 5,192 valid stale drains, with zero failures or duplicate live streams. Crash-point coverage over 5,000 seeds observed before-sync 3,306, after-sync-before-send 3,376, and after-send 3,318. Exact `Save → send → apply → Advance` and Save-error no-send/no-apply/no-Advance behavior passed review and regression tests. Mutation testing killed 14/15 probes; the surviving non-production mutation shows the Apply-error test does not itself assert no `Advance`, although production correctly returns. Other non-blocking smells: an unreachable nil-trace branch would dereference a nil node; random loss/dup rates cap at 30% while scripted schedules can use 100%; generator v3 serializes stateful fault intervals, reducing overlap richness; `make corpus` remains intentionally vacuous until 4B. Evidence checks only the first five build-path boxes and the out-of-order and duplication canaries. |
| 2026-07-15 | 0A | `phase-0/0A` / `b9a3f3ca4e526ce37339b179b44a2ff197326acb` (baseline `7194ed8`) | PASS after one correction cycle — GPT-5.6-Terra, high effort | Unconditional PASS — Claude Opus 4.8 (`claude-opus-4-8`), high effort, session `8e518c63-e19a-4ecf-b599-293b58081ff1`; read-only, no fallback | Orchestrator and verifier: `make proto` byte-identical; `go build ./...`; `go vet ./...`; `golangci-lint run` (0 issues); `go test ./... -race`; `make boundary`; focused boundary, raft defaults, storage, clock, unary gRPC transport, server lifecycle/fail-stop, and daemon tests; `make sim SEED=123`; `make corpus`; `go doc -all ./internal/raft`; daemon start/tick/SIGTERM exit 0; all PASS. Fresh local clone reproduced the CI-equivalent gates. Negative controls independently planted forbidden `time.Now()` and a bare `go` statement, observed boundary failures, reverted, and re-ran green. CI link: N/A — local-only repository by instruction. |
| 2026-07-15 | 1A | `phase-1/1A` / `f97563888db330bd4d38d2533c0fc2eab5bf7282` | PASS — GPT-5.6-Terra, xhigh effort, no fallback | Packet verifier not required for this M packet; orchestrator packet audit PASS; independent Phase 1 exit verifier PASS (see exit row) | `go test ./internal/raft -count=20`; `go test ./internal/raft -race -count=10`; `go build ./...`; `go vet ./...`; `golangci-lint run` (0 issues); `go test ./... -race -count=1`; `make boundary`; `go doc -all ./internal/raft`; all PASS. Evidence covers the first 7 Phase 1 build-path boxes and first 2 unit validation boxes. |
| 2026-07-15 | 1B | `phase-1/1B` / `599a853`, integrated correction `d660686` | PASS — Claude Sonnet 5 (`claude-sonnet-5`), high effort, no fallback; an initial context-contaminated session was discarded before edits, then a clean implementation and fresh correction cycle completed | Packet verifier not required for this M packet; orchestrator packet audit PASS; independent Phase 1 exit verifier PASS (see exit row) | `go test ./sim -race -count=3`; focused membership, stale-tick, and determinism regressions with `-race -count=20`; `make sim SEED=123`; `go build ./...`; `go vet ./...`; `golangci-lint run` (0 issues); `go test ./... -race -count=1`; `make boundary`; `git diff --check`; all PASS. Evidence covers Phase 1 sim build-path boxes only; scenario, validation, and exit boxes remained open until packet 1C and exit verification. |
| 2026-07-15 | 1C | `phase-1/1C` / `42415df83f885ce8d13c0f33c9ff832779f842ca` | PASS — GPT-5.6-Terra, high effort, no fallback | Orchestrator packet audit PASS; independent Phase 1 exit verifier PASS (see exit row) | Each cold-start/30s-steady, leader-crash, 1\|1\|1, 2\|1, heal, and disruptive-rejoin scenario passed; combined scenario suite passed with `-race -count=20`; deterministic replay passed with `-race -count=100`; `make sim-500`; `make sim SEED=505`; `go build ./...`; `go vet ./...`; `golangci-lint run` (0 issues); `go test ./... -race -count=1`; `make boundary`; `make corpus`; `make proto` byte-identical; all PASS. Worker and orchestrator independently enabled double-voting: both observed the invariant fail at seed 4 with term 1 leaders `[2 3]`, restored the canonical rule, and re-ran 500 seeds green. Seed 505 showed disruptive rejoin: isolated follower term 1→8, convergence at term 9 after heal. Evidence covers all eight Phase 1 validation boxes. |
| 2026-07-15 | Phase 1 exit | `main` / verified `21bae787f482bb2bb94d54e4799d58a84acf2350` | Packets PASS — 1A GPT-5.6-Terra xhigh; 1B Claude Sonnet 5 high; 1C GPT-5.6-Terra high; no fallbacks | Unconditional PASS — GPT-5.6 Sol (`gpt-5.6-sol`), xhigh effort, read-only, no fallback, thread `019f696e-dfd3-7791-bbae-e058cf5565c8` | Fresh remote-free checkout: all named scenarios passed with `-race -count=20`; core `-race -count=100`; determinism `-race -count=100`; `make sim-500`; full build/vet/lint/race; Phase 0 boundary and focused negative detectors; storage/clock/gRPC/server/daemon regressions; Ready order/fail-stop; full-membership/even-quorum; crash/restart/stale-tick tests; `make proto` byte-identical; daemon start/tick/SIGTERM exit 0; all PASS. Independent disposable double-vote control failed as required at seed 4, term 1, leaders `[2 3]`; target remained clean. Non-blocking smells recorded: real-host `Step`/`Tick` serialization, real `fixedRand`, restart-without-crash misuse, ignored sim read errors. CI link: N/A — local-only repository by instruction. |
| 2026-07-16 | 2B | `phase-2/2B` / `a7e2a72ef60a83b3e4b26f10f8c6ce15b2663149` (contract correction `3db267f`) | PASS — GPT-5.6 Terra (`gpt-5.6-terra`), high effort, no fallback, after a frozen-contract escalation. The initial by-value `Apply(raftpb.Entry)` attempt reproducibly failed `go vet` copylocks twice; its uncommitted evidence is preserved at `stash@{0}`. Richard approved guide v1.4 and the pointer contract before fresh redispatch. | Packet verifier not required for this S packet; orchestrator diff/API/ownership audit PASS; independent Phase 2 exit verification pending | Worker and orchestrator: focused state-machine tests with `-race` (orchestrator `-count=20`); exact `Apply(*raftpb.Entry)` interface inspection; `go build ./...`; `go vet ./...`; `golangci-lint run` (0 issues); `go test ./... -race`; `make boundary`; `make sim SEED=1`; deterministic replay (`-count=10` orchestrator); `make sim-500`; `make corpus`; `git diff --check`; all PASS. Evidence covers only the StateMachine/mapsm build-path box; cross-leader dedup remains for packet 2D. |
| 2026-07-16 | 2A | `phase-2/2A` / original `ca33cf6`, corrected `a21220b572d4928a62c04deeb1d55f4060712d21` (contract correction `d56eadd`) | PASS after one safety-correction cycle — GPT-5.6 Sol (`gpt-5.6-sol`), max effort, no fallback. The first verifier found a delayed duplicate empty-probe response could be credited to a newer suffix request; Richard approved additive `AppendEntriesResp.matchIndex`, the guide/phase contract was corrected, and a fresh worker implemented exact request-covered acknowledgements. | Unconditional PASS — Claude Fable 5 (`claude-fable-5`), max effort, read-only, no fallback, session `aba4b1a9-9fa0-4f96-ac7f-51b0d7757464` | Worker, orchestrator, and verifier: focused duplicate/coverage/out-of-range regressions `-race -count=100`; full raft `-race -count=100`; Figure 8 `-race -count=50`; Phase 2 replication sim `-race -count=20`; determinism `-race -count=100`; `make sim-500`; `make sim SEED=20260716`; fresh full `go test ./... -race -count=1`; build/vet/lint (0 issues); boundary; corpus; whitespace; public API and AST/import audit; protobuf regeneration twice with byte-identical output; all PASS. Disposable negative 1 dropped the current-term commit predicate and Figure 8 failed on the old-term-commit assertion. Disposable negative 2 restored unsafe in-flight attribution and the delayed-duplicate regression failed with false `matchIndex=2` and `commitIndex=2`. Target remained clean. Evidence covers the first eight replication build-path boxes and the consistency, delayed-duplicate, Figure 8, and NOOP validation boxes only. Non-blocking smells: duplicate rejections may cause redundant retry backoff; `Advance`-without-`Ready` is a latent host footgun to re-check in Phase 3; frozen leaderCommit re-emission can delay propagation by one heartbeat; corpus remains vacuous until Phase 4. |
| 2026-07-16 | 2C | `phase-2/2C` / original `134f978`, corrections `dc6bc57`, `d507473`, final `a3281ce368f988687d713d61119cfa73fc5d7d49` | PASS after three correction cycles — Claude Sonnet 5 (`claude-sonnet-5`), high effort, sessions `a23c921a-8301-49d3-824f-0a8f8daa1ed0`, `0b6bfec9-1a98-462d-8a5f-20594871909b`, and `d6e671ec-7e09-4341-ac0d-a691476ffe18`; final escalation correction Claude Fable 5 (`claude-fable-5`), max effort, session `4b172601-0a3b-4111-b631-076b0c9e5123`. An exact Sol xhigh correction invocation stopped before reading/editing because it could not attest the assigned specific model; no model was silently substituted, and the documented Fable fallback was used. | Final unconditional PASS — GPT-5.6 Sol (`gpt-5.6-sol`), xhigh effort, read-only, no fallback, thread `019f6d31-865d-7521-aa0a-043a969b2dff`. Earlier fresh Sol verifiers (`019f6c52-b4e7-79b2-adc5-c4bc3b4bc920`, `019f6d12-702c-7c13-a7e3-b4ec92c3ecf6`) correctly rejected stranded single- and multi-waiter fail-stop paths; each finding was returned to a fresh worker. | Worker, orchestrator, and final verifier: full diff/protected-tree/frozen-API audit; build/vet/lint (0 issues); full `go test ./... -race`; server, gRPC transport, mqctl, and sim race stress up to 100×; boundary plus forbidden-import/Go-statement self-tests; `make sim SEED=20260716`; `make sim-500`; corpus; exact protobuf regeneration with byte-identical output; live three-process put/get/delete and kill-leader read-after-failover; all PASS. Independent real-Raft three-node overlays proved Save/Apply fail-stop drains every earlier `context.Background()` waiter exactly once, preserves already-applied successes, prevents post-stop registration, empties both maps, and keeps mqctl client ID/sequence stable across `Unavailable` retries. Negative control disabled only `notifier.FailStop(err)` and both Save/Apply regressions failed after 5 seconds with blocked waiters; canonical target stayed clean. Evidence marks only the three 2C build-path boxes. Packet 2D validation/exit boxes remain open. Non-blocking smells: `make corpus` is vacuous until Phase 4; mqctl's 50 retries have no backoff; Phase 2 storage is intentionally volatile. |
| 2026-07-16 | 2D | `phase-2/2D` / `00ddbb110eb381c6fa9675efccef83dd8cbcec80` (baseline `462967f`) | PASS — GPT-5.6 Sol (`gpt-5.6-sol`), xhigh effort, no fallback, thread `019f6d41-2e56-7cc2-b6cf-b16474a92c3b`. One transient vet/lint copylocks finding in new validation helpers was corrected before commit; final worktree clean. | Unconditional PASS — Claude Fable 5 (`claude-fable-5`), max effort, read-only target, no fallback, session `2d318f44-be02-4fdc-8d68-0819ae145842`; independently reviewed both packet 2D and the complete Phase 2 exit. | Worker, orchestrator, and verifier independently passed `go build ./...`, `go vet ./...`, integration-tag vet, `golangci-lint run` (0 issues), `go test ./... -race -count=1`, `make boundary`, `make sim SEED=20260716`, `make sim-500`, `make corpus`, `make proto-check` with byte-identical output, and `make real-smoke`; focused sim/dedup/GET, waiter/Ready, hash, and boundary race repetitions also passed. The 500-seed corpus exercised replication, old-leader isolation, successor election/proposal, and healing with SingleLeaderPerTerm plus automatic LogMatching after every event. Three independent real smokes killed acked-write leaders 1, 3, and 1 respectively; replacements 3, 1, and 2 served the value. Worker, orchestrator, and verifier each dropped only the Figure 8 current-term commit clause in disposable worktrees, observed the unsafe old-term commit failure, restored, re-ran green, and removed the clean scratch tree. Protected contracts, protobufs, frozen APIs, existing assertions, checklist, and RUNLOG were untouched by the packet; the boundary detector change is a strict strengthening. Non-blocking observations: the simulator's Apply-error branch lacks a sim-specific failing-state-machine test but the identical host contract is covered; committed-prefix checking uses synchronous `LastApplied`; `sim-500` now names the Phase 2 corpus while the Phase 1 corpus remains in the full suite. Accepted limitations remain volatile storage and an unbounded dedup table. |
| 2026-07-16 | Phase 2 exit | `main` / verified `00ddbb110eb381c6fa9675efccef83dd8cbcec80` | Packets PASS — 2A GPT-5.6 Sol max; 2B GPT-5.6 Terra high; 2C Claude Sonnet 5 high with Fable 5 max final correction; 2D GPT-5.6 Sol xhigh; no silent substitutions | Unconditional PASS — Claude Fable 5 (`claude-fable-5`), max effort, read-only target, no fallback, session `2d318f44-be02-4fdc-8d68-0819ae145842` | Complete Phase 2 diff and frozen-contract audit; build/vet/lint; full race regression; Phase 0 boundary and negative-detector tests; fixed seed and ≥500 Phase 1+2 invariant corpus; Figure 8 positive and independently mutated negative control; delayed-duplicate acknowledgement safety; exact-command cross-leader dedup; `(index,term)` waiter collision; GET-through-Apply partition behavior; deterministic hash replay; Ready Save/send/apply/Advance and fail-stop regressions; byte-identical protobuf regeneration; race-built real three-process mqctl round-trips and acked-write failover; all PASS. All Phase 2 build, validation, and exit boxes are supported and checked. CI link: N/A — local-only repository by instruction. |
| 2026-07-16 | 3B | `phase-3/3B` / initial `c4731b60283b4619b4806ebdc6243d1dad260637`, corrected `40d7fb4`, merged to `main` as `801f729` (baseline `c3875df`) | PASS after one verification correction — primary GPT-5.6 Terra xhigh could not attest the assigned model in two clean no-edit attempts; documented Sol max fallback likewise stopped before reading/editing; exact Claude Fable 5 (`claude-fable-5`) max fallback implemented the packet, session `6a1aa501-c566-43d6-a9ed-498eed86e80d`. After the verifier found missing logical-truncate prefix fidelity, a fresh exact Fable 5 max worker corrected it in commit `40d7fb4`; no silent substitution. | Initial FAIL then final unconditional PASS — GPT-5.6 Sol (`gpt-5.6-sol`), max effort, read-only, no fallback. The first pass found that overlapping Saves omitted 3A's `TruncateRecord`; final thread `019f6e26-19ea-7020-b743-e3c0f35ada9b` confirmed the correction. | Worker, orchestrator, and final verifier: full scope/protected-tree audit; build/vet/lint (0 issues); boundary race; focused CrashStorage/FaultSchedule race stress (orchestrator/verifier 50x); full `go test ./... -race`; fixed seed `20260716`; `make sim-500`; corpus command (checker still vacuous before Phase 4); protobuf regeneration with no drift; clean worktrees. Every byte cut of an overlapping HardState→Truncate→Entries batch is tested: a surviving truncate plus torn entries recovers a shortened old log, synced bytes remain identical, ordinary re-replication succeeds, and identical runs produce identical platter bytes. All three crash points, Save ordinals across recovery, arbitrary unsynced-prefix retention, and synced-byte immunity remain green. Only the Phase 3 sim-storage build-path box is checked; integration, crash matrix, validation, and exit boxes remain open for 3C. Non-blocking smells: sim and disklog use separate codecs; index zero remains permissive only in the sim's invalid-input domain; `make corpus` is intentionally vacuous until Phase 4. |
| 2026-07-17 | 3A | `phase-3/3A` / initial `783090c`, durability correction `057dfe0`, v1.6 framing correction `f5d1214d2c1eeff4c1aa8b7fea8e5f08f3ea6158`, merged to `main` as `2646288` | PASS after contract escalation and correction cycles — Claude Fable 5 (`claude-fable-5`) max implemented the initial disklog and recovery-durability correction. An independent review proved the raw `[len][crc][payload]` tail classifier information-theoretically ambiguous; Richard approved fixed-width nibble framing in guide v1.6. Fable then hit its provider limit, so the documented GPT-5.6 Sol (`gpt-5.6-sol`) max fallback implemented the exact corrected contract, thread `019f6ed3-b3b2-73d2-bff8-6dea656a1aeb`; no silent substitution. | Unconditional PASS — Claude Opus 4.8 (`claude-opus-4-8`), high effort, read-only, no fallback, session `f9c5493e-90ea-4bae-a611-d7847e7b5046`. It independently audited the complete diff and confirmed the narrowly migrated final-frame assertions are stricter, not weakened. | Worker, orchestrator, and verifier: focused disklog normal/race suites; exhaustive every-prefix final-frame matrix; every complete-middle-frame physical position × all 255 replacement values; final observed-terminator parity/length/CRC/protobuf rejection; accepted final-terminator ambiguity; all 256 codec byte values; 32-bit checked-size compile; append/read, truncation, rotation, directory/file recovery sync, poisoning, and short-write review; `make proto-check`; build/vet/lint (0 issues); boundary plus forbidden-import/Go-statement self-tests; full `go test ./... -race`; whitespace and clean-tree checks; all PASS. Evidence checks the first six 3A build-path boxes and first four Phase 3 validation boxes only. Server wiring, disklog/sim equivalence, crash matrix, restart/dedup/real-process gates, and all exit boxes remain open for 3C/phase exit. Non-blocking smell: torn-tail recovery may sync the final segment twice; accepted framing limit is that a missing final terminator or one changed to an alphabet byte is indistinguishable from a genuine interrupted append. |
| 2026-07-17 | 3C | `phase-3/3C` / escalation evidence `99d5e4c`, final `d37cf851deb3f7b69b729d1f62e2dc8d4a65b5e5`, merged to `main` as `016ccb978c7052ba27b2762f16fee49c60a3d822` (baseline `0b59f35`) | PASS after worker escalation — assigned GPT-5.6 Sol (`gpt-5.6-sol`) max, thread `019f7262-35ed-7b31-85f1-6a2a137655c3`, failed twice and preserved its state without a model substitution; documented cross-provider Claude Fable 5 (`claude-fable-5`) max completed the packet, session `48a151ed-e53c-4bf6-90fa-d13b646d369f`. The correction replaced a hard-coded `(index,term)` crash oracle with the actual sentinel and strengthened crash-fired, volatile-reset, replay, dedup, prefix-equivalence, and real SIGKILL evidence. | Unconditional PASS — GPT-5.6 Sol (`gpt-5.6-sol`), max effort, read-only, no fallback, thread `019f72b1-93a3-71f1-85ed-c580d2c1873d`. A prior fresh verifier was invalidated because its workspace sandbox denied localhost binds; the corrected unrestricted fresh verifier reran every gate and found no Critical/High/Medium/Low issues. | Worker, orchestrator, and corrected verifier independently audited frozen APIs, protected files, unary ack-paced InstallSnapshot, recovery without commit persistence/special replay, and exact `Save → send → apply → Advance` fail-stop behavior. All passed: build; ordinary and integration vet; lint (0 issues); protobuf byte-identical regeneration; boundary plus forbidden-import/Go-statement/category self-tests; complete disklog suite including all final prefixes and every middle-frame byte × 255 substitutions; server/recovery tests; 36-case role/crash-point matrix; same-seed replay; positive vote persistence; all 60 unsynced prefixes; all-node restart storm; dedup reconstruction; `sim-crash-500`; `sim-500`; Figure 8/current-term and cross-leader dedup; `real-smoke`; actual leader SIGKILL mid-`mqctl` followed by same-data-dir restart with all 10 acknowledged writes retained; and full `go test ./... -race -count=1`. Disposable send-before-persist controls failed as required with term 1 leaders `[1 2]`, were restored byte-identically, and reran green. Packet evidence supports the two remaining build-path boxes and all seven remaining validation boxes; exit boxes remain open for the fresh integrated Phase 3 verifier. Informational smell: Figure 8 and cross-leader dedup use ordinary simulator `MemStorage`; the disklog claim is supported compositionally by prefix equivalence, production disklog wiring, the real SIGKILL gate, and independent disklog/regression suites. |
| 2026-07-17 | Phase 3 exit | `main` / verified `92464b38582f35feedf25df60b595a4304a801fb` (opening baseline `c3875df`) | Packets PASS — 3A Claude Fable 5 max with documented GPT-5.6 Sol max framing fallback; 3B assigned GPT-5.6 Terra xhigh unavailable for attested execution, completed by documented Claude Fable 5 max fallback; 3C assigned GPT-5.6 Sol max failed twice, completed by documented Claude Fable 5 max fallback; no silent substitutions | Unconditional PASS — GPT-5.6 Sol (`gpt-5.6-sol`), max effort, read-only, no fallback, thread `019f732a-7bf5-70c1-9421-e1095363dbfb`. A separate exact launch that declined its own model-label attestation was stopped before verification actions and was not used. | Complete 21-file integrated diff and locked-contract audit PASS; unary, ack-paced InstallSnapshot preserved; frozen public APIs/protobuf fields and `internal/raft` boundary preserved; exact Ready ordering and Save-error fail-stop confirmed. PASS: clean-tree/diff checks; build; ordinary and integration vet; lint (0 issues); boundary and all forbidden-import/bare-Go detector self-tests; byte-identical protobuf regeneration with protoc 35.1, protoc-gen-go 1.36.11, and protoc-gen-go-grpc 1.6.2; Linux/386 disklog compile; complete disklog suite with every final cut `0..48`, every complete-middle-frame byte × 255 replacements, all corruption/torn-tail/rotation/truncation/sync/poisoning/recovery/prefix-equivalence cases; server/recovery/Ready tests; every unsynced cut `0..59`; 36 role × crash-point cases; same-seed reproducibility; restart storm; recovery dedup; current-term/Figure 8/cross-leader dedup; `make sim-crash-500`; `make sim-500`; fixed-seed sim; corpus command; `make real-smoke`; actual leader SIGKILL after 3 acknowledgements, same-data-dir restart, and all 10 acknowledged writes retained; `make test`; and `go test ./... -race -count=1` (sim 157.865s). Independent disposable send-before-persist negative failed as required with term 1 leaders `[1 2]`; restoration matched SHA-256 `9edb466aa70f44366914e722fb78650e534d36fad6651a2f8c5187361328866a` and reran green. All four Phase 3 exit boxes are supported and checked. Non-blocking findings: the disklog association for Figure 8/cross-leader dedup is compositional rather than a single direct disklog-backed test; one non-required `gofmt -d` probe found a one-line method in `sim/crashstorage.go`; newly creating a top-level data directory relies on caller/deployment durability of its parent entry; `make corpus` remains vacuous until Phase 4. CI link: N/A — local-only repository by instruction. |
